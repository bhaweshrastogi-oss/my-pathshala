# Copy to .env.local for `vercel dev` (never commit real secrets)

PHONEPE_ENV=production
PHONEPE_CLIENT_ID=SU2512261330453294206129
PHONEPE_CLIENT_SECRET=221fb003-b76e-4669-9e1b-325e2dcdab86
# From PhonePe dashboard (sandbox often uses "1")
PHONEPE_CLIENT_VERSION=1

# Comma-separated website origins allowed to call the API and used in redirect_url
# Example:
CORS_ORIGINS=https://pmpathshala.com,https://www.pmpathshala.com,http://localhost:5500

# Local testing only — allows any Origin / redirect (never enable in production)
# PHONEPE_ALLOW_INSECURE_CORS=1
